# Control Matrix

This will grow to ~60+ rows as remaining sections of the ASIC ERS
readiness scope are built. Only controls that actually exist in code
today are listed as anything other than "Not implemented" — no row here
is aspirational.

| # | Risk | Regulatory/operational objective | Preventive control | Detective control | System implementation | Control owner | Frequency | Evidence generated | Test performed | Status | Remaining gap |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | Real customer money moves before ASIC/AFCA/insurance/banking are all in place | No production activation without every mandatory regulatory gate satisfied | `launch_gates.is_production_authorized()` called at every real-money code path (interface defined; not yet wired into onboarding/payment endpoints) | `launch_gates.get_all_gate_statuses()` dashboard view (backend function exists; no admin UI yet) | `backend/launch_gates.py`, `backend/migrations/012_pilot_config_and_launch_gates.sql` | Compliance lead (TBD — named owner not yet assigned in this repo) | Continuous (evaluated on every call) | `launch_gate_audit_log` append-only table | `test_pilot_config_and_launch_gates.py` — 12 checks covering fail-closed default, maker-checker per gate, expiry auto-close, two-person activation | Implemented and tested (logic only — no gate has real evidence yet, so production remains correctly blocked) | Not yet called from any real API endpoint; no admin UI to submit/approve evidence; the 22 real-world evidence items themselves (ASIC exemptions, AFCA membership, insurance, banking, legal opinion, etc.) are all External dependency / Requires legal confirmation |
| 2 | Pilot product limits (customer cap, credit limit, aggregate exposure) drift or are bypassed by a config change | Configuration can never exceed the ERS-notified pilot envelope, enable a prohibited capability, or be self-approved | `pilot_config.validate_config_change()` (app layer) + matching `CHECK` constraints in migration 012 (DB layer) | N/A yet — no scheduled job re-validates the active config against the hard ceilings after the fact (defence in depth item, not yet built) | `backend/pilot_config.py`, `backend/migrations/012_pilot_config_and_launch_gates.sql` | Product/compliance lead (TBD) | On every proposed config change | `pilot_config_versions` table (immutable, versioned) | `test_pilot_config_and_launch_gates.py` — 18 checks covering every hard ceiling, prohibited capability, and maker-checker path | Implemented and tested | Not yet wired to any admin UI for proposing/approving a config change; no scheduled re-validation job; `real_money_enabled` on a config row is documented as not itself authoritative (launch_gates is) but nothing yet asserts that at runtime with a combined check |
| 3 | Segregated customer funds / credit funding / operating accounts | Customer funds never finance credit or pay operating expenses | Ledger account-type separation (`customer`, `TRUST_BANK`, `OPERATING`, etc.) + deferred DB trigger balance enforcement | `reconciliation.py` internal check (ledger vs. sum of customer balances) | `backend/ledger.py`, `backend/reconciliation.py`, migration `002_ledger_and_reconciliation.sql` | Prior session's work, not this session's — carried over | Per transaction (posting-time) + daily (reconciliation) | Ledger postings (immutable), reconciliation run records | `backend/test_ledger_flow.py` | Implemented but awaiting external configuration | This ledger predates the pilot config/launch-gate system and is not yet integrated with `pilot_config`'s per-customer/aggregate limits or gated by `launch_gates`; external bank balance check (`_fetch_external_trust_balance()`) is a stub |

*(Rows 4+ to be added as onboarding, responsible-lending, bill verification, complaints/AFCA, document versioning, and the remaining sections are built.)*
